# Hermes 工作区规范（v3 通用版）

> **本文件是 SOUL.md 中"系统目录结构"的详细展开**，
> 必须与 SOUL.md 八条铁律保持一致：**数据归位、日志集中、不臃肿、不碰系统目录**。
> 目录架构的任何增删修改，**必须经主人同意**后方可执行。
>
> **v3 升级（2026-06-04）**：从 v1 老版 6 层信息流升级到 11 步

---

## 一、目录架构（已锁定）

```
{工作区路径}/                        # 默认 ~/.hermes/
│
├── memories/           记忆档案
│   ├── MEMORY.md      中期记忆（我的笔记）
│   ├── USER.md        主人画像
│   └── TODO.md        待办真实源
│
├── data/              业务数据 【约束：仅此处存放业务数据】
│   ├── {业务模块 1}/  如 investment / novel / office ...
│   ├── {业务模块 2}/
│   └── {业务模块 3}/
│
├── docs/              系统文档与主人文档
│   ├── *.md           部署手册、运维手册、巡检手册等
│   └── [{沟通平台}文档备份]  重要文档在 {沟通平台}云端同步存储
│
├── skills/            技能目录
│   ├── [内置技能]     Agent 内置技能
│   └── [用户自建]     主人根据需要创建的技能
│
├── tools/             自定义脚本与工具
│
├── logs/              系统日志 【约束：日志必须在此】
│
├── cron/              定时任务配置与输出
│
├── mcp-servers/       MCP 服务（飞书等）
│
├── cache/             临时缓存（图片/截图等，7天自动清理）
│
├── sessions/          会话历史（系统管理，自动压缩）
│
├── {系统代码目录}/    Agent 核心代码 【约束：严禁存放主人数据】
│   └── （默认 hermes-agent/）
│
├── bin/               系统可执行文件
├── platforms/         平台配置
├── sandboxes/         沙箱环境
└── [根目录状态文件]   pid / state.db 等，由系统自动管理
```

> ⚠️ **占位符说明**：
> - `{工作区路径}` = 默认 `~/.hermes/`，可改为 `/data/hermes/` 等
> - `{业务模块}` = `investment` / `novel` / `office` / `project-x` 等
> - `{沟通平台}` = `飞书` / `Telegram` / `Discord` / `微信` 等
> - `{系统代码目录}` = `hermes-agent/`（默认）

---

## 二、目录职责表

| 目录 | 职责 | 存放内容 | 约束 |
|------|------|---------|------|
| `memories/` | 记忆 | `MEMORY.md` / `USER.md` / `TODO.md` | — |
| `data/` | 业务数据 | 各业务模块子目录 | ❗严禁放其他类型文件 |
| `docs/` | 文档 | 部署/运维/巡检手册、`{沟通平台}`文档备份 | — |
| `skills/` | 技能库 | 内置技能 + 用户自建技能 | — |
| `tools/` | 用户工具 | 自定义脚本 | — |
| `logs/` | 系统日志 | agent.log / errors.log 等 | ❗日志必须在此 |
| `cron/` | 定时任务 | 任务配置与输出文件 | — |
| `mcp-servers/` | MCP 服务 | 飞书等平台的服务端 | — |
| `cache/` | 临时缓存 | 图片/截图/下载等 | ❗超 7 天自动清理 |
| `sessions/` | 会话历史 | jsonl 会话文件 | ⚙️ 由系统自动管理 |
| `{系统代码目录}/` | 系统代码 | Agent 核心代码 | ❌ 严禁存放主人数据 |

---

## 三、关键路径速查

| 查找目标 | 路径 |
|---------|------|
| 业务数据 | `{工作区}/data/{业务模块}/` |
| 系统文档 | `{工作区}/docs/` |
| 记忆档案 | `{工作区}/memories/` |
| 技能库 | `{工作区}/skills/` |
| 系统日志 | `{工作区}/logs/` |
| 会话历史 | `{工作区}/sessions/` |
| MCP 服务 | `{工作区}/mcp-servers/` |
| 用户工具 | `{工作区}/tools/` |
| 记忆数据库 | `{工作区}/memory_store.db`（Holographic） |

---

## 四、禁止事项

根据 SOUL.md 八条铁律：

- ❌ 不在 `{系统代码目录}/` 下存放任何主人数据
- ❌ 不在 `{工作区}/` 之外创建主人数据目录（顶层散落）
- ❌ 不创建未列入本规范的顶层目录
- ❌ 业务数据不放进 `data/` 以外的任何目录
- ❌ 日志不放进 `logs/` 以外的任何目录

---

## 五、架构变更规则

> **锁定原则**：上述目录架构经主人确认后锁定。
> - 任何增、删、改目录结构的行为，必须先征得主人同意
> - 临时文件路径 `/tmp/{系统名}-*` 不需要申请，但应遵循命名规范

---

## 六、特殊情况豁免条款

> **系统平稳运行优先**：以下情况可暂不受上述约束，但事后应主动记录并汇报。

1. **系统运行保障**：为保证系统平稳运行而必须创建的临时目录或文件（如 lock 文件、socket、pid 文件）
2. **自动生成物**：系统自动生成的状态文件（state.db、gateway.pid、auth.json 等）
3. **升级迁移**：系统版本升级时，系统自动创建的兼容性子目录
4. **外部依赖**：第三方工具（如 nix、docker、conda 等）在 `{工作区}/` 内自动生成的目录

豁免判断优先级：**系统稳定性 > 路径规范**，但事后应主动告知主人。

---

## 七、v3 升级标注（2026-06-04）

> **本段标注 v3 升级完成 + 关联升级文件 + 提示其他 session 此版本已升级到 11 步信息流。**

### 升级要点

- **v3 升级** = 信息流定义从"v1 老版 6 层"升级到"11 步信息流"（指令→Agent→大脑→图书馆 5 层→工具决策→处理→反馈→迭代）
- **目录结构无变化**（本升级**只动**信息流定义和系统代码模块，**不动**目录结构）

### 路径合规验证

本次升级**未违反** AGENTS.md 任何"四、禁止事项"条款：

- ✅ 未在 `{系统代码目录}/` 下存放任何主人数据
- ✅ 未在 `{工作区}/` 之外创建主人数据目录
- ✅ 未创建未列入本规范的顶层目录
- ✅ 业务数据未放进 `data/` 以外的目录
- ✅ 日志未放进 `logs/` 以外的目录

### 11 步信息流（v3 唯一权威定义）

```
1. 指令
2. Agent
3. 大脑（方法论，Holographic fact_store）
4. 图书馆 5 层（按顺序，不跳步）
   4.1 踩坑经验（vault/踩坑记录/）
   4.2 知识库（vault/entities/ → vault/）
   4.3 近期对话（session_search）
   4.4 缓存点（data/{模块}/cache/ mtime+TTL）
   4.5 网络搜索（web_search）
5. 判断调用哪种工具（5 步全自动）
6. 处理数据/信息/咨询
7. 结果/反馈（store_feedback 闭环）
8. 迭代/完善方法论（fact_store 存回大脑或图书馆）
```

### 强约束

其他 session 读到此标注 → 严格按 11 步走完整路径，**不**跳任何一步，**不**直接进入第 8 步分析。

---

## 八、占位符速查

部署本通用版到新机器时，**必替换**的占位符：

| 占位符 | 默认值 | 替换为 |
|---|---|---|
| `{工作区路径}` | `~/.hermes/` | 实际工作区路径 |
| `{沟通平台}` | 飞书 | 你使用的沟通平台 |
| `{系统代码目录}` | `hermes-agent/` | 系统代码目录名 |
| `{业务模块}` | investment / novel / office | 你的实际业务模块 |
| `{主人称呼}` | 主上 | 你希望 Agent 怎么称呼你 |
| `{平台名}` | feishu | 实际平台代码（feishu/telegram/discord） |
| `{用户名}` | （具体值） | 当前登录用户名 |

**注意**：占位符是**通用选项**，**不是**真实信息。**不要**把真实信息直接写进文件。

---

**v3 升级 = 11 步信息流（v3 唯一权威定义）** —— 所有 Agent / Skill / 子系统都必须按本文件执行。
