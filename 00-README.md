# DIKW 记忆系统 v3（通用版）

> **本目录是 4 个文件组成的完整部署包**：另一台 Hermes Agent clone 整个目录 → 5 分钟内即可拥有 11 步信息流 v3 升级版 DIKW 记忆系统。
>
> **v3 = 信息流定义从 v1 老版 6 层升级到 11 步**（指令→Agent→大脑→图书馆 5 层→工具决策→处理→反馈→迭代）

---

## 📁 目录结构

```
通用版_v3/
├── 00-README.md                    # 本文件（总览 + 快速开始）
├── 01-记忆系统使用指南.md           # DIKW 4 层 + 11 步信息流
├── 02-SOUL.md                       # Agent 行为规则 + 信息检索流水线
└── 03-AGENTS.md                     # 工作区目录规范
```

---

## 🚀 5 分钟快速开始

### 步骤 1：检查依赖

- Hermes Agent v0.5.0+（含 Holographic 记忆插件）
- Python 3.11+
- 配置文件：`~/.hermes/config.yaml`（含 `plugins.hermes-memory-store` 配置段）

### 步骤 2：复制文件

```bash
# 复制到你的 Hermes 工作区
cp -r 通用版_v3/* ~/.hermes/

# 或者选择性复制（推荐）
cp 02-SOUL.md ~/.hermes/SOUL.md              # 替换 Agent 行为规则
cp 03-AGENTS.md ~/.hermes/AGENTS.md          # 替换目录规范
mkdir -p ~/.hermes/data/knowledge/vault/00-系统文档/
cp 01-记忆系统使用指南.md ~/.hermes/data/knowledge/vault/00-系统文档/记忆系统使用指南.md
```

### 步骤 3：配置占位符

打开 `02-SOUL.md` 和 `03-AGENTS.md`，把所有 `{占位符}` 替换为你的实际值：

| 占位符 | 含义 | 示例 |
|---|---|---|
| `{主人称呼}` | 你希望 Agent 怎么称呼你 | 主上 / 老板 / 名字 / 昵称 |
| `{沟通平台}` | Agent 接收消息的平台 | 飞书 / Telegram / 微信 / Discord |
| `{工作区路径}` | Hermes 工作区根目录 | `~/.hermes` |
| `{平台名}` | 当前平台 | feishu / telegram / discord |
| `{用户名}` | 当前用户名 | `john` |

### 步骤 4：验证

```bash
# 启动 Hermes Agent
hermes --tui

# 发送测试消息（飞书示例）
"用 11 步信息流查一下我的 DIKW 笔记"
```

如果 Agent 严格按 11 步走（指令→Agent→大脑→图书馆 5 层→工具决策→处理→反馈→迭代），**v3 升级成功**。

---

## 🎯 11 步信息流（v3 唯一权威定义）

```
1. 指令
   ↓
2. Agent
   ↓
3. 大脑（方法论，Holographic fact_store）
   命中 → 进入第 5 步工具决策
   未命中 ↓
4. 图书馆 5 层（按顺序，不跳步）
   4.1 踩坑经验（vault/踩坑记录/）
   4.2 知识库（vault/entities/ → vault/）
   4.3 近期对话（session_search）
   4.4 缓存点（data/{模块}/cache/ mtime+TTL，根据需要及时更新数据）
   4.5 网络搜索（web_search）
   任意层命中 → 进入第 5 步工具决策
   ↓
5. 判断调用哪种工具（5 步全自动）
   5.1 扫描 available_skills → skill_view(name) 加载
   5.2 选对工具（分析→execute_code / 改配置→patch / 查源码→search_files / 网络→web_search / 搜索→fact_store / 文件→read_file）
   5.3 查可用性（注册≠激活，显式检查——踩坑：fact_feedback 从未被调用）
   5.4 批量优先（3+次同类调用→execute_code 合并）
   5.5 安全验证（破坏性操作先 --dry-run / backup / 确认）
   ↓
6. 处理数据/信息/咨询
   ↓
7. 结果/反馈
   ↓
8. 迭代/完善方法论（fact_store 存回大脑或图书馆）
```

**强约束**：听到"信息流/检索/查找"类指令 → 严格按 1→2→3→4.1→4.2→4.3→4.4→4.5→5→6→7→8 走，**不**跳任何一步，**不**直接进入第 8 步分析。

---

## 📊 4 个文件关系图

```
00-README.md（本文件）
   ↓
01-记忆系统使用指南.md ← DIKW 4 层模型 + 11 步信息流（v3 唯一权威）
   ↓
02-SOUL.md ← Agent 行为规则（铁律 + 思维协议 + 信息检索流水线）
   ↓
03-AGENTS.md ← 工作区目录规范（目录架构 + 关键路径 + 禁止事项）
```

**部署顺序**：先读 README（本文件）→ 复制 4 个文件 → 替换占位符 → 验证。

---

## 🔧 4 个文件核心内容速查

### 01-记忆系统使用指南.md
- DIKW 4 层模型（Data / Information / Knowledge / Wisdom）
- 11 步信息流（v3 唯一权威）
- 11 条检索原则
- Holographic 插件使用详解
- HRR 关键词编码策略
- 时效性管理
- 反馈校准

### 02-SOUL.md
- 三层人格框架（模型 / 记忆 / Agent）
- 8 条铁律（P0-P7）
- 思维协议（反降智三问 + 执行前验证）
- 信息检索流水线（7+1 层 → 11 步）
- 记忆系统 DIKW 分流四问
- 工具选用速查

### 03-AGENTS.md
- 目录架构（{工作区} = `~/.hermes/`）
- 目录职责表
- 关键路径速查
- 禁止事项
- 架构变更规则
- 特殊情况豁免

---

## 🛡️ 通用版审计 6 步（推送公开仓库前必跑）

部署本目录到新机器**不**需要审计（直接复制即可）。**如果**你要把本目录推到 GitHub 公开仓库，**必跑**：

```bash
# 1. grep 敏感词
SENSITIVE="主上\|小明\|用户223475\|{主人原称呼}\|{主人原平台}"
grep -rn "$SENSITIVE" --include="*.md" .

# 2. 区分占位符 vs 真实信息
#    占位符示例（如"飞书 / Telegram / 微信"）→ 可保留
#    真实私人信息（如"主上：用户223475"作项目维护者）→ 必须改

# 3. 路径硬编码检测
grep -rn "/home/[^/]*/" --include="*.md" .

# 4. 个人信息嗅探
grep -rn "@gmail\|@qq\|@163" --include="*.md" .

# 5. 占位符验证：所有 {xxx} 是否都是"通用选项"
grep -rn "{[a-z_]\+}" --include="*.md" .

# 6. 整体过审：所有 4 个文件是否通用？
#    - 任何用户 clone 后能直接用？
#    - 是否含个人偏好/具体配置/具体路径？
#    - 占位符是否都是通用选项？
```

**审计不通过 → 不推到公开仓库**。

---

## 📝 关联 fact（已写入原 Holographic 部署时使用 fact_store 索引）

部署到新机器后，可选地把这些 fact 写入你的 Holographic 大脑：

- fact_8044：信息流 DIKW CIRAAF 反模式（lesson）
- fact_8045：主上原话 8 步定义（general，待升级到 11 步）
- fact_8046：v3 升级完成（lesson）
- fact_8047：信息流 11 步不跳步（lesson）
- fact_8048：源码级强制 vs 文档级承诺（lesson）

---

## 🆘 常见问题

### Q1: 11 步信息流和老版 6 层有什么区别？
A: 老版 6 层缺"工具决策"层 + "反馈闭环"层 → Agent 检索到信息后**不知道用什么工具** + **不知道如何反馈**。11 步补全这两层。

### Q2: 必须用 Holographic 吗？
A: **是**。Holographic 是 Hermes 内置插件，零外部服务依赖。Hindsight 等其他 provider 需自行适配。

### Q3: 4 个文件冲突怎么办？
A: 部署前**先备份** `~/.hermes/SOUL.md` / `AGENTS.md` / `记忆系统使用指南.md`。如果冲突，本目录是"通用模板"——按需选择性复制。

### Q4: 占位符怎么替换？
A: 用编辑器（VSCode / vim）全局替换。例如 `{主人称呼}` → `老板`。

---

## 📜 版本变更

- **v3.0.0 (2026-06-04)**：从 v1 老版 6 层升级到 11 步信息流（指令→Agent→大脑→图书馆 5 层→工具决策→处理→反馈→迭代）
- **v1.0.0 (2026-06-01)**：初版，6 层信息流

---

## 📜 许可

本目录采用 MIT 协议。任何用户 clone / fork / 修改后即可使用，无需保留原作者署名。

---

**v3 升级 = 11 步信息流（v3 唯一权威定义）** —— 其他 Agent 拿到本目录后**立即拥有**完整的 DIKW 记忆系统 v3 能力。
